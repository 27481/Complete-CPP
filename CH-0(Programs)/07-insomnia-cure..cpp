#include<iostream>
#include<bits/stdc++.h> 
using namespace std;
int main(){
    int n; // n = no of pages 
    cin>>n;

    while(n--){
        string s;
        cin>>s;

        if(s.size()>10){
            cout<<s[0]<<s.size()-2<<s.size()-s.[s.size()-1]<<endl;
        }
        else cout<<s<<endl;
    }
    return 0;
}

// 
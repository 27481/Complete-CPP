// !character type data is used to store characters works under single quote

//? A character type variable can store only a single character  if you want to store multiple characters like your name and address  then we will call it string a.k.a a set of characters is called a string 

// In order to represent string we have a dataType called string

#include<iostream>
#include<bits/stdc++.h> 
using namespace std;
int main(){
    char ch='a';
    cout<<ch<<"="<<(int)ch <<endl;
    return 0;
}
